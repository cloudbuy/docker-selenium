{{- if .Values.firefoxNode.noVnc.ingress.enabled -}}
{{- $name := include "seleniumGrid.firefoxNode.fullname" . }}
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: {{ $name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: selenium-firefox-node
    {{- include "seleniumGrid.commonLabels" . | nindent 4 }}
    {{- with .Values.customLabels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
{{- with .Values.firefoxNode.noVnc.ingress.annotations }}
  annotations: {{- toYaml . | nindent 4 }}
{{- end }}
spec:
{{- if .Values.firefoxNode.noVnc.ingress.tls }}
  tls:
  {{- range .Values.firefoxNode.noVnc.ingress.tls }}
    - hosts:
      {{- range .hosts }}
        - {{ . }}
      {{- end }}
      secretName: {{ .secretName }}
  {{- end }}
{{- end }}
  rules:
  {{- range .Values.firefoxNode.noVnc.ingress.hosts }}
    - host: {{ . }}
      http:
        paths:
          - path: /
            backend:
              serviceName: {{ $name }}
              servicePort: novnc-http
  {{- end }}
{{- end }}


{{- if .Values.edgeNode.noVnc.ingress.enabled -}}
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: {{ template "seleniumGrid.edgeNode.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: selenium-hub
    {{- include "seleniumGrid.commonLabels" . | nindent 4 }}
    {{- with .Values.customLabels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
{{- with .Values.edgeNode.noVnc.ingress.annotations }}
  annotations: {{- toYaml . | nindent 4 }}
{{- end }}
spec:
{{- if .Values.edgeNode.noVnc.ingress.tls }}
  tls:
  {{- range .Values.edgeNode.noVnc.ingress.tls }}
    - hosts:
      {{- range .hosts }}
        - {{ . }}
      {{- end }}
      secretName: {{ .secretName }}
  {{- end }}
{{- end }}
  rules:
  {{- range .Values.edgeNode.noVnc.ingress.hosts }}
    - host: {{ . }}
      http:
        paths:
          - path: /
            backend:
              serviceName: {{ template "seleniumGrid.edgeNode.fullname" . }}
              servicePort: novnc-http
  {{- end }}
{{- end }}

